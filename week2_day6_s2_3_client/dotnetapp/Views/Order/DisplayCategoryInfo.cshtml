<!-- @model List<CategoryInfoViewModel>

<h2>Category Information</h2>

<table class="table">
    <thead>
        <tr>
            <th>Category Name</th>
            <th>Product Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model)
        {
            <tr>
                <td>@category.CategoryName</td>
                <td>@category.ProductCount</td>
            </tr>
        }
    </tbody>
</table> -->
